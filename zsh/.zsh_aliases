# system aliases
alias s='sudo'
alias up='sudo apt update && sudo apt upgrade -y'

# git aliases
alias gs='git status'
alias gd='git diff --name-only'
alias gdc='git diff --cache --name-only'
alias gds='git diff --staged --name-only'
alias ga='git add'
alias gaa='git add .'
alias gc='git commit -m'
alias gp='git push'
alias gm='git merge'

# kali
alias kex='kex --win -s'
alias kexs='kex --sl -s'

# neovim
alias nv='nvim'
alias delsw='find . -type f -name "*.sw[klmnop]" -delete'
